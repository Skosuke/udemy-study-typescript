export{};

type ObjectType = {
    name: string;
    age: number;
}

interface ObjectInterFace {
    name: string;
    age: number;
}

let object: ObjectInterFace = {
    name: 'Kosuke',
    age: 30
}